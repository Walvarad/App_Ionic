import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'primer_proyecto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
